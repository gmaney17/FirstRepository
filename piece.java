/**
 * Every piece on the board is of type piece, has methods which
 * every piece does
 */
public class piece {
   public boolean isWhite;

   public piece(boolean w) {
      this.isWhite = w;
   }
}
